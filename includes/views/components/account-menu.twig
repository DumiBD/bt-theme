{% if menu_items %}
    <div class="pills pills--light">
        {% for item in menu_items %}
            <a class="pills__item {{ item.current ? 'active' : '' }}" href="{{ item.url | raw }}">
                {{ item.title }}
            </a>
        {% endfor %}
    </div>
{% endif %}
